# 双目相机 FPS 测试 + IMU 时间戳同步 —— 进展与问题总结

> 日期：2026-06-04（初版）/ 2026-06-09（更新：C 版落地，60fps 零丢帧达成）
> 目标：测出双目相机（DECXIN / Nori 3D）在**最高分辨率下的最高 FPS**，并**同步采集 IMU 数据、做好时间戳同步**。

> **最新结论（2026-06-09）**：已在 Windows 本机用 C 版（`stereo_imu_sync_c/`）跑通，
> 4000×1200 **满 60fps、30s 录制左右各 1805 帧零丢帧**、IMU 601Hz、帧↔IMU 时间戳同一时钟域。
> 详见下方 **第 6、7 节**。

---

## 1. 设备与厂商资料结论

| 项目 | 结论 |
| --- | --- |
| 相机型号 | DECXIN-3261V1/3362V1 模组 + Nori 3D 方案，VID:PID = `1bcf:2d50` |
| 传感器 | 两颗 1920×1200 全局快门 sensor 水平拼接，单路 USB3.0 输出 |
| 整幅分辨率 | **4000×1200** = 有效图像 + 底部编码区 |
| **最高分辨率最高帧率** | **4000×1200 @ 60fps，必须用 MJPEG**；YUYV 在此分辨率只有 30fps（USB3 带宽上限） |
| 驱动 | USB UVC 免驱（Win/Linux/Mac/Android） |
| IMU | TDK ICM42688，默认 ±4g / ±1000dps |
| 时间戳同步 | 帧时间戳=曝光结束(EE)，IMU=测量完成时刻，**同一时钟域、单位 µs、32bit 会溢出**；IMU↔帧同步 < 30µs |
| 同步数据载体 | 每帧图像底部编码区：12 组×16 字节。第 1 组=帧曝光起止时间戳；第 2~12 组=IMU 时间戳+数据 |

**关键点**：图像帧和 IMU 共用相机内部同一个微秒计数器，所以**不需要主机侧软件对齐**——解码出编码区后，帧曝光时间和 IMU 时间天然在同一根时间轴上。

---

## 2. 已完成的工作

代码位于 `stereo_imu_sync/`（已同步复制到 Windows 的 `C:\cam_test\`）：

| 文件 | 说明 | 状态 |
| --- | --- | --- |
| `tst_decode.py` | 厂商 C++ 解码逻辑（`icm_decode.cpp`/`icm42688_decode.h`）的 Python 移植：从图像底部编码区解出帧曝光时间戳 + IMU 采样 | ✅ 已用厂商样例 BMP 验证正确 |
| `capture_test.py` | 取流主程序：MJPG 4000×1200@60 取流、实测 FPS、逐帧解码 IMU、时间戳同步、导出 CSV；跨平台（Linux v4l2 / Windows msmf·dshow） | ✅ 逻辑已验证 |
| `probe_formats.py` | 多分辨率取流探测脚本 | ✅ |
| `README.md` / `requirements.txt` | 运行说明与依赖 | ✅ |

**解码验证结果**（用厂商样例 `4000x1200_0_0_10.bmp`，作为 ground truth）：
- 解出 `group_size=12`（对应"每帧 12 组"）✅
- 帧曝光时长 `7.49ms`（符合规格"最大曝光 8.33ms"）✅
- 11 条 IMU 采样，时间戳单调递增、间隔 ~1.66ms（≈600Hz）✅
- 加速度 Z ≈ -1g（重力）、陀螺仪为静止微动小值 ✅
- IMU 时间戳区间正好横跨帧曝光区间 ✅
- 已处理：BMP/相机帧上下方向差异（自动探测）、32bit µs 回绕

---

## 3. 遇到的问题：WSL2 透传取流失败 ❌

为了在当前 WSL2 环境里直接取流，做了 USB 透传：

1. Windows 端 `usbipd-win` 安装 → `usbipd list` 找到相机 BUSID `3-6` → `bind` + `attach --wsl` 成功。
2. WSL 内：`/dev/video0`、`/dev/video1` 正常出现（该 WSL 内核已自带 UVC 驱动，**无需重编内核**）。
3. 取流测试：相机**接受了 `4000×1200@60 MJPG` 配置**，但 `select() timeout`，**一帧都读不到**。
4. 用 `probe_formats.py` 从 640×480 一路试到 4000×1200，**所有分辨率、所有格式都是 0 帧 TIMEOUT**。

### 根因

- UVC 摄像头视频流走 **USB isochronous（同步）传输**。
- **usbipd / WSL 的 USB 转发对 isochronous 传输支持不完整**，所以控制指令（设置分辨率/帧率）能下发，但视频数据流传不过来。
- **与带宽无关**（连 640×480 都 0 帧），是协议层硬限制，降分辨率也无效。
- 同时确认：相机本身支持 4000×1200@60（配置被接受），**问题完全出在 WSL 透传链路，不是相机或代码**。

---

## 4. 下一步要做什么 ✅

**结论：放弃 WSL 透传，改在 Windows 本机直接跑（UVC 免驱，无需 usbipd）。代码已就绪。**

### 步骤 1：把相机从 WSL 还给 Windows
```powershell
usbipd detach --busid 3-6
```

### 步骤 2：Windows 本机运行
```powershell
python --version                       # 确认有 Python，没有则先安装
pip install numpy opencv-python
cd C:\cam_test
python capture_test.py --list                                   # 确认相机索引
python capture_test.py -d 0 --mode rawfps --seconds 10          # 测最高分辨率最高 FPS 上限
python capture_test.py -d 0 --mode sync   --seconds 10 --out out # 取流+IMU+时间戳同步, 出 CSV
# 若 msmf 读不到帧, 换后端:
python capture_test.py -d 0 --backend dshow --mode rawfps --seconds 10
```

### 验收要点
- `rawfps` 输出里 `[相机实际生效]` 应为 `4000x1200 @ 60 fourcc=MJPG`；看「实测平均 FPS」「相机时间戳 FPS」「帧间隔抖动」是否稳定在 60。
- `sync` 会在 `C:\cam_test\out\` 生成：
  - `frames.csv`：`frame_idx, host_t_s, cam_exp_start_us, cam_exp_end_us, exposure_us, n_imu`
  - `imu.csv`：`frame_idx, cam_exp_end_us, imu_t_us, acc_xyz(mg), gyro_xyz(dps)`
  - 其中 `cam_exp_end_us`（帧时间戳）与 `imu_t_us` 同一时钟域，可直接对齐/插值。

### 备选方案（如必须用 Linux）
- 在**原生 Linux 主机**（非 WSL）上直接插相机跑 `capture_test.py`（v4l2 直连，无 isochronous 转发问题）。

---

## 5. 待确认 / 风险（2026-06-04 时；下方第 6 节已逐条闭环）

- Windows 上是否已安装 Python（未确认）。
- Windows MSMF 后端对 4000×1200@60 MJPG 的支持需实测；不行则用 DShow 后端。
- 编码区在相机实时帧里的上下方向已做自动探测，但仍需用真机首帧确认解码成功率。

---

## 6. C 版落地 + Windows 本机实测（2026-06-09）✅

为同时录**双路 H.265 视频**并做 IMU 同步，改用 C 实现（`stereo_imu_sync_c/`，基于 FFmpeg
`libav*` + dshow 取流 + AMD 硬件编码 `hevc_amf`）。Python 版思路（编码区解 IMU/时间戳）
完整移植到 C。

- 取流后端用 **dshow**（设备名 `DECXIN Camera`），4000×1200@60 MJPEG 取流正常。
- 编码区上下方向**自动探测**成功（本机首帧判定"需翻转"），IMU 解码成功率 **100%**。
- 录完 `.hevc` 裸流，再用 `hevc2mp4` 无损 remux 成 `.mp4`。
- 一键脚本：`stereo_imu_sync_c/record_session.sh <秒数>`（录制 + 自动转 MP4）。

### 掉帧问题排查与解决（关键）

首版只有 **~40fps**（sensor 实际 60fps），即采集端跟不上、丢帧。逐层定位（程序结束会
打印各级每帧平均耗时）并修复：

| 瓶颈 | 现象 | 解决 |
| --- | --- | --- |
| ① 单线程 MJPEG 解码 | 4000×1200 解一帧 ~32ms ≫ 16.67ms 预算；该解码器不支持 slice 多线程 | **多个独立解码器实例并行**（MJPEG 帧内编码各帧独立）+ 按序号重排，解码等效降到 ~6ms |
| ② 切图+NV12 串在采集线程 | 两次 `sws_scale` ~26ms | **下放到编码侧并行** |
| ③ sws 与编码串在一条线程 | 合计 ~17–20ms ＞预算，长录累积丢帧 | **拆成两级独立线程**（转换 / 编码），各级单帧都在预算内 |
| ④ `yuvj420p` 触发 sws 范围转换慢路径 | sws 偏慢 | 按 `yuv420p` 处理（数值原样打包，画面无差异），命中无缩放快速拷贝 |

最终流水线：`采集(主) → N×解码 → 重排 → 收集(IMU/CSV+克隆) → 左右各[转换→编码]`。
采集主线程只读 USB 包、阻塞入队、分配序号后绝不丢（重排无空洞），过载由 dshow 512M 缓冲兜底。

**默认 `--dec-threads 4`**（实测甜点，给 CPU 热降频留足余量）。换机器若仍丢帧，看结束时
打印的各级耗时，哪级超 16.67ms 就针对性加线程。

### 实测结果（30s，本机 16 核）

| 指标 | 结果 |
| --- | --- |
| 采集帧数 | **1805 帧 / 30.1s ≈ 60fps** |
| 左/右编码 | 各写出 **1805 帧，零丢帧** |
| 各级每帧耗时 | USB 16.6ms · 解码等效 5.6ms · 收集 0.2ms · sws 6.4ms · 编码 4.2ms（均 ≪ 16.67ms） |

输出目录：`stereo_imu_sync_c/recordings/rec_20260609_180137_4000x1200_60fps/`
（`left/right.mp4`、`left/right.hevc`、`frames.csv`、`imu.csv`、`preview_left/right.png`）。

---

## 7. 时间戳同步核验（2026-06-09）✅

对上面这组 30s 录制做了完整核验：

| 项目 | 结果 |
| --- | --- |
| 帧序号连续性 | `0..1804` **连续，0 空洞**（无丢帧） |
| 帧时间戳帧率 | **60.096 fps**，帧间隔恒定 16640µs，**抖动 0µs**（相机硬件计数器） |
| IMU 速率 | 19855 行，**601 Hz**，时间戳单调、0 回退（跨帧有 1 个重叠样本属正常） |
| 帧↔IMU 时钟域 | `frames.csv` 的 `cam_exp_end_us` 与 `imu.csv` 的 `imu_t_us` 同一 µs 计数器；每帧约 11 条 IMU，时间区间横跨该帧曝光窗口，可直接对齐/插值 |
| 立体视差 | 左右预览帧中近景物体水平位移不同 → 左右 sensor 切分正确（画面旋转 90° 为相机侧装的物理朝向） |

> CSV 字段：
> - `frames.csv`：`frame_idx, host_t_s, cam_exp_start_us, cam_exp_end_us, exposure_us, n_imu`
> - `imu.csv`：`frame_idx, cam_exp_end_us, imu_t_us, acc_xyz(mg), gyro_xyz(dps)`
> 程序已处理 32bit µs 回绕。
